# tiny

A **tiny** example package used to prove the BrewDocs build pipeline.

## Install

```bash
npm install tiny
```

## Usage

Call `brew()` to start. It is *fast*.

- No config needed
- Works offline
- Ships zero dependencies

## Notes

This README is intentionally small. Later phases extract JSDoc, exported
functions, and `package.json` metadata into a full one-page site.
